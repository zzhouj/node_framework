{
    "title": "{{TITLE}}",
    "port": "{{PORT}}",
    "mysql": {
        "host": "{{DB_HOST}}",
        "port": "{{DB_PORT}}",
        "database": "{{DB_NAME}}",
        "user": "{{DB_USER}}",
        "password": "{{DB_PASSWD}}",
        "debug": false,
        "connectionLimit": 10
    },
    "redis": {
        "host": "{{RDS_HOST}}",
        "port": "{{RDS_PORT}}"
    },
    "rootSecret": "{{ROOT_SECRET}}",
    "pageSize": 20
}
